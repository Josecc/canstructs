{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.1",
    "nx": "^14.7.8"
  },
  "scripts": {
    "publish": "lerna publish --no-private",
    "build": "npx lerna run build",
    "version": "lerna version --no-private"
  },
  "targetDefaults": {
    "build": {
      "dependsOn": [
        "^build"
      ]
    }
  }
}
